Name = "itheima is a brand of itcast"
count = 0
for u in Name:
    if u == "a":
        count += 1
        print(f"字符串中有{count}个a")

字符串中有1个a
字符串中有2个a
字符串中有3个a
字符串中有4个a


Name = "itheima is a brand of itcast"
count = 0
for u in Name:
    if u == "a":
        count += 1
print(f"字符串中有{count}个a")
字符串中有4个a